import fs from "fs";\nimport path from "path";\nimport { JSDOM } from "jsdom";\n\ndescribe("components/TodoList.html", () => {\n    let dom;\n    beforeAll(() => {\n        const htmlContent = fs.readFileSync(path.resolve(__dirname, "../../components/TodoList.html"), "utf8");\n        dom = new JSDOM(htmlContent);\n    });\n\n    it("should contain a template with id 'todo-list-template'", () => {\n        const template = dom.window.document.getElementById("todo-list-template");\n        expect(template).not.toBeNull();\n    });\n\n    it("should contain a style tag", () => {\n        const style = dom.window.document.querySelector("style");\n        expect(style).not.toBeNull();\n    });\n});\n